{
  "name": "prefab-monorepo",
  "version": "1.0.0",
  "description": "PREFAB Design and Marketplace Platform",
  "main": "index.js",
  "private": true,
  "scripts": {
    "heroku-prebuild": "echo 'Skipping shared file copy for API deployment'",
    "heroku-postbuild": "cd src/api && npm install --production=false && npm run build",
    "start": "cd src/api && npm start"
  },
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  },
  "cacheDirectories": [
    "src/api/node_modules"
  ]
} 